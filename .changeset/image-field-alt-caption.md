---
"emdash": minor
"@emdash-cms/admin": minor
---

Adds alt text and caption inputs to image fields in the entry editor, matching the rich-text image block panel. The inputs default to the media record's alt text and caption when an image is attached, and editors can override them per entry.
